import * as Joi from "joi";

// export const Statistic_params_joi = Joi.object({
//     limit: Joi.number().min(5).required(),
//     page: Joi.number().min(1).required(),
//     start_date: Joi.string().optional(),
//     end_date: Joi.string().optional()
// }).unknown(true);